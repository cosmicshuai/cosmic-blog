---
layout: base.njk
---

<!-- Reading progress — a thin phosphor trace at the top of the tube. -->
<div class="fixed inset-x-0 top-0 z-[60] h-[2px] bg-transparent" aria-hidden="true">
  <div id="progress" class="h-full w-0 bg-p shadow-bloom transition-[width] duration-100 ease-out"></div>
</div>

<article class="pt-2">

  <!-- ══ FILE HEADER ═══════════════════════════════════════════════ -->
  <header class="mb-14">
    <nav class="mb-6 font-mono text-xs text-p-faint" aria-label="Breadcrumb">
      <a href="/" class="transition-colors hover:text-p">~</a>
      <span class="text-line-2" aria-hidden="true">/</span>
      <a href="/posts/" class="transition-colors hover:text-p">posts</a>
      <span class="text-line-2" aria-hidden="true">/</span>
      <span class="text-p-dim">{{ page.fileSlug }}.md</span>
    </nav>

    <h1 class="aberrate max-w-[22ch] font-display text-[clamp(2.25rem,7vw,4.5rem)] leading-[0.95] text-p">
      {{ title }}
    </h1>

    {% if description %}
      <p class="mt-5 max-w-measure font-mono text-base leading-relaxed text-p-dim">{{ description }}</p>
    {% endif %}

    <!-- stat(1) -->
    <dl class="mt-8 grid grid-cols-2 gap-px border border-line bg-line sm:grid-cols-4">
      {% set meta = [
        { k: "written", v: date | readableDate },
        { k: "read", v: content | readingTime + " min" },
        { k: "words", v: content | wordCount },
        { k: "mode", v: "-rw-r--r--" }
      ] %}
      {% for m in meta %}
        <div class="bg-bg px-3 py-2.5">
          <dt class="font-pixel text-[10px] uppercase tracking-widest text-p-faint">{{ m.k }}</dt>
          <dd class="mt-1 font-mono text-sm text-p-hi">{{ m.v }}</dd>
        </div>
      {% endfor %}
    </dl>

    {% if tags %}
      <div class="mt-4 flex flex-wrap gap-2">
        {% for tag in tags %}
          {% if tag != "posts" %}
            <a href="/tags/{{ tag | slugify }}/" class="tag">#{{ tag }}</a>
          {% endif %}
        {% endfor %}
      </div>
    {% endif %}
  </header>

  <!-- ══ BODY ══════════════════════════════════════════════════════ -->
  <div class="post-content">
    {{ content | safe }}
  </div>

  <!-- ══ EOF ═══════════════════════════════════════════════════════ -->
  <footer class="mt-20 border-t border-line pt-10">

    <p class="mb-10 font-mono text-sm text-p-faint">
      <span class="text-p/60">$</span> EOF — {{ content | wordCount }} words, {{ content | readingTime }} min
    </p>

    <div class="panel bracketed group mb-10">
      <div class="panel-hd">
        <span><span class="text-p/50" aria-hidden="true">▚▚</span> author</span>
        <span class="text-p/50">uid 1000</span>
      </div>
      <div class="panel-body pool flex items-center gap-5">
        <span aria-hidden="true" class="flex h-14 w-14 shrink-0 items-center justify-center border border-line bg-bg-deep/50 font-display text-3xl leading-none text-p glow-sm">
          {{ site.author | first | upper }}
        </span>
        <span class="min-w-0">
          <a href="/about/" class="block font-mono text-sm text-p-hi transition-colors hover:text-p">{{ site.author }}</a>
          <span class="mt-0.5 block font-mono text-xs text-p-dim">{{ site.role }}</span>
        </span>
        <a href="/feed.xml" class="btn-ghost ml-auto hidden shrink-0 sm:inline-flex">subscribe</a>
      </div>
    </div>

    {% set previousPost = collections.posts | getPreviousCollectionItem(page) %}
    {% set nextPost = collections.posts | getNextCollectionItem(page) %}

    {% if previousPost or nextPost %}
      <nav class="grid gap-4 sm:grid-cols-2" aria-label="Adjacent records">
        {% if previousPost %}
          <a href="{{ previousPost.url }}" class="group panel bracketed pool block p-4 transition-colors hover:border-p/50">
            <span class="font-pixel text-[10px] uppercase tracking-widest text-p-faint">◂ previous</span>
            <span class="mt-2 block font-mono text-sm text-p-hi transition-colors group-hover:text-p">{{ previousPost.data.title }}</span>
          </a>
        {% else %}<span class="hidden sm:block"></span>{% endif %}

        {% if nextPost %}
          <a href="{{ nextPost.url }}" class="group panel bracketed pool block p-4 text-right transition-colors hover:border-p/50">
            <span class="font-pixel text-[10px] uppercase tracking-widest text-p-faint">next ▸</span>
            <span class="mt-2 block font-mono text-sm text-p-hi transition-colors group-hover:text-p">{{ nextPost.data.title }}</span>
          </a>
        {% endif %}
      </nav>
    {% endif %}

    <div class="mt-8 flex flex-wrap gap-3">
      <a href="/posts/" class="btn-ghost"><span aria-hidden="true">←</span> archive</a>
      <a href="/" class="btn-ghost">index</a>
    </div>
  </footer>
</article>
